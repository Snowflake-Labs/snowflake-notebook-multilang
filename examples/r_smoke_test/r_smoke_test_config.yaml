# =============================================================================
# R Smoke Test -- Combined Dependencies
# =============================================================================
# Single config for the R smoke test notebook. Covers session context,
# EAI settings, R environment, and package sources.
#
# Usage (single cell):
#   from sfnb_setup import setup_notebook
#   setup_notebook(config="r_smoke_test_config.yaml")
#
# All sections below are OPTIONAL -- sensible defaults are used when omitted.
# =============================================================================

# -- Session context (optional) -----------------------------------------------
# If omitted, the session's existing database/schema/warehouse are used.
# context:
#   warehouse: "MY_WH"
#   database: "MY_DB"
#   schema: "MY_SCHEMA"

# -- EAI settings (optional) --------------------------------------------------
# managed: name of an EAI you own and allow the script to modify.
# supplementary_name: name for auto-created EAI (default: MULTILANG_NOTEBOOK_EAI).
# If omitted, EAIs are auto-discovered via SHOW EXTERNAL ACCESS INTEGRATIONS.
# eai:
#   managed: "MY_EAI"
#   supplementary_name: "MULTILANG_NOTEBOOK_EAI"

# -- Custom mirrors (Artifactory / Nexus / air-gapped environments) -----------
# Uncomment and set these to route all package downloads through your
# internal artifact repository proxy. Works with JFrog Artifactory,
# Sonatype Nexus, AWS CodeArtifact, or any standard proxy.
#
# mirrors:
#   conda_channel: "https://artifactory.example.com/conda-forge-remote"
#   pypi_index: "https://artifactory.example.com/api/pypi/pypi-remote/simple"
#   cran_mirror: "https://artifactory.example.com/cran-remote"
#   micromamba_url: "https://artifactory.example.com/generic-tools/micromamba/linux-64/latest"
#   ssl_cert_path: "/etc/ssl/certs/corporate-ca-bundle.crt"

# -- Model Registry conda channel policy (optional) ---------------------------
# Force all sfr_log_model() conda_dependencies to resolve from a specific
# channel (e.g. conda-forge) instead of the default Snowflake Anaconda Channel.
# Set conda_channel_strict: true to prevent users from overriding the channel.
#
# registry:
#   conda_channel: "conda-forge"
#   conda_channel_strict: true

# -- Language and package spec ------------------------------------------------
env_name: "workspace_env"

languages:
  r:
    enabled: true
    r_version: "4.5.2"
    conda_packages:
      # Shared
      - r-cli
      - r-rlang>=1.0.0
      - r-reticulate>=1.25
      # snowflakeR
      - r-tidyverse
      - r-dbplyr
      - r-httr2>=1.0.0
      - r-lazyeval
      # RSnowflake
      - r-dbi>=1.2.0
      - r-jsonlite
      - r-dplyr>=1.1.0
    cran_packages: []
    addons:
      adbc: false
      duckdb: false
    tarballs:
      snowflakeR: "https://github.com/Snowflake-Labs/snowflakeR/releases/download/v0.1.0/snowflakeR_0.1.0.tar.gz"
      RSnowflake: "https://github.com/Snowflake-Labs/RSnowflake/releases/download/v0.2.0/RSnowflake_0.2.0.tar.gz"

  scala:
    enabled: false

  julia:
    enabled: false
